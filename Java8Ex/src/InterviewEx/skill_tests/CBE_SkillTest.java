package InterviewEx.skill_tests;

import java.math.BigDecimal;
import java.math.RoundingMode;

public class CBE_SkillTest {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		
		  int i1 = 2;
		     
		     int i2 = 5;
		     
		     double d = 3 + i1 /i2 + 2;
		     
		     System.out.println("d: " + d);
		     
		     String s = " ";
		     
		     System.out.println("s: " + s.getClass().getName());
		     
		     RoundingMode mode = RoundingMode.CEILING;
		     BigDecimal big1 = new BigDecimal(-11);
		     BigDecimal big2 = new BigDecimal(2);
		     System.out.println("Big divide answewr:" + big1.divide(big2, mode));
		     
		/*default:
		    assert false : suit;
		If the suit variable takes on another value and assertions are enabled, the assert will fail and an AssertionError will be thrown.

		An acceptable alternative is:

		default:
		    throw new AssertionError(suit);*/
		     
		     /**
		      * 
		      * Outer.Inner    Outer.CreateInner(); -- method returning static Inner class 
		      * 
		      * -- Static Inner method -- fill lines creating itself
		      * 
		      */
		     
		     // Security All permissions , Custom security
		     // SAX interfaces
		     // Swing -- Data, 
		     // main(String... args)
		     // Interface methods and static
		     // ConcurrentModificationError
		     // List size needs to be limited -- How??
		     // Line count in a file
		     // Swing components and synchronization
		     // Enhanced for loop and Iterator for the collection object
		     // 

	}

}
